#include<stdio.h>
#define MAXCOL 10
#define MAXROW 10
int vector[MAXROW][MAXCOL];
main(){
	int row,col,count;
	printf("\nEnter the number of rows:");
	scanf("%d",&row);
	printf("\nEnter the number of columns:");
	scanf("%d",&col);
	printf("\nEnter the of non-zero elements:");
}
